# With skill

You are completing a small R coding task. Read the user's prompt, write the
requested file(s) into the current working directory, and stop. Do not run
code.

Before writing code, consult the skill at:

  ~/.agents/<skill-name>/SKILL.md

Apply its conventions where they are relevant to the task.

If your opencode setup loads skills from `~/.agents` automatically, this file
just opts the task into using them. Otherwise, read the SKILL.md above
yourself before writing the solution.
